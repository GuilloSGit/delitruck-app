specVersion: "v1"
serviceId: "delitruck-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "delitruck-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
